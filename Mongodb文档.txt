MongoDB:

	skus(sku):
		title 					String 		sku名称
 		stock					NUMBER		库存量
 		id 						String 		sku唯一id
 		sku_merchant_code 		String  	商品型号编码
 		attr_ids				String 		多级SKU属性id(这里的id和title中的名称对应) 764651-L, 764647-蓝色
 		price					String 		价格
		img 					String 		sku图URL

	cates（分类）:
		sort_num				NUMBER		分类排序
		cate_id					String 		商品分类id
		cate_name				String 		分类名称
		parent_id				NUMBER 		父类目id				没有父ID,说明是顶级分类
		status 					NUMBER(1)  	分类的状态
		created_time			String 		添加时间
		updated_time			String		更新时间
		url_key 				String 		点击分类的链接

	goods_detail:
		item_id					String		item_id

	Favorite:		产品收藏信息表
		_id: 				ObjectId("5a31e3cabfb7ae29c040a042"),
	   	product_id			580835d0f656f240742f0b7c",
	   	user_id				NumberInt(383),                   			// customer表的id
	   	created_at			NumberInt(1513219018),
	   	updated_at		 	NumberInt(1513219018),
	   	store 				fecshop.appfront.fancyecommerce.com/cn"  	// store的key

	product（商品表）
		itemid 					STRING 			商品id
		created_time			String			创建时间
		created_user_id			用户id
		update_time				String			更新时间
		item_desc				String 			商品描述
		item_name				STRING 			商品名称
		fx_fee_rate				String 			分销分成比例
		seller_id				String 			卖家id
		imgs 					ARRAY 			商品图片(展示商品的图片/视频)
			[{url:'aaa.img'},{url:'bbb.img'}]
		merchant_code 			STRING 			商品编号
		sold 					NUMBER 			商品销量
		thumb_imgs 				ARRAY 			商品缩略图

		skus 					ARRAY 			商品sku，无型号的商品skus为空
			[{skuid:112233},{skuid:224644}]

		status 					STRING 			商品状态（onsale ：销售中 instock：已下架 delete：已删除）
		stock 					NUMBER 			库存量
		remote_free_delivery 	NUMBER 			偏远地区是否包邮：1不包邮，0包邮
		free_delivery 			NUMBER 			是否包邮：0不包邮，1包邮
		price 					STRING 			商品价格
		titles 					ARRAY 			商品图片描述
		cates 					ARRAY			商品分类

	product_detail(商品详情)
		goods_detail_id			NUMBER
		item_id					NUMBER 			商品ID
		text					STRING 			type=1填写此字段，详情文本
		type 					NUMBER 			type =1 文字模块,type =2 图片, type =3 商品模块，type=4视频模块
		pos 					NUMBER 			记录某一个detail对象在detail中的位置
		faceurl	 				String 			首页地址
		video_type 				String 			1表示腾迅视频，2表示美拍视频(type=4填写此字段)
		url 					STRING 			图片的url（type=2填写此字段）



	attrs(商品模型,一个模型有多个属性)
		attr_title		String 		属性名称
		attr_values		Array 		属性值数组

	attr(属性)
		attr_id 			Number   	属性ID		（因为为了生成sku需要知道属性的值）
		attr_value			String 		属性值


Mysql:
	customer（用户信息）:
	 	'id' 						int(20) 		unsigned NOT NULL AUTO_INCREMENT,
	  	'password_hash' 			varchar(80) 	DEFAULT NULL COMMENT '密码',
	  	'password_reset_token' 		varchar(60) 	DEFAULT NULL COMMENT '密码token',
	  	'email' 					varchar(60) 	DEFAULT NULL COMMENT '邮箱',
	  	'name' 						varchar(100) 	DEFAULT NULL,
	  	'is_subscribed' 			int(5) 			NOT NULL DEFAULT '2' COMMENT '1代表订阅，2代表不订阅邮件',
	  	'auth_key' 					varchar(60) 	DEFAULT NULL,
	  	'status' 					int(5) 			DEFAULT NULL COMMENT '状态',
	  	'created_at' 				int(18) 		DEFAULT NULL COMMENT '创建时间',
	  	'updated_at' 				int(18) 		DEFAULT NULL COMMENT '更新时间',
	  	'password' 					varchar(50) 	DEFAULT NULL COMMENT '密码',
	  	'access_token' 				varchar(60) 	DEFAULT NULL,
	  	'birth_date' 				datetime 		DEFAULT NULL COMMENT '出生日期',
	  	'favorite_product_count' 	int(15) 		NOT NULL DEFAULT '0' COMMENT '用户收藏的产品的总数',
	  	'type' 						varchar(35) 	DEFAULT 'default' COMMENT '默认为default，如果是第三方登录，譬如google账号登录注册，那么这里的值为google',
	  	'access_token_created_at' 	int(20) 		DEFAULT NULL COMMENT '创建token的时间',
	  	'allowance' 				int(20) 		DEFAULT NULL COMMENT '限制次数访问',
	  	'allowance_updated_at' 		int(20) 		DEFAULT NULL,
	  	'phone'						varchar(20) 	COMMENT '手机号'
	  	PRIMARY KEY ('id'),
	  	UNIQUE KEY 'access_token' ('access_token')

	 customer_address（用户地址）:
	 	  'address_id' 			int(15) NOT NULL AUTO_INCREMENT,
		  'email' 				varchar(155) DEFAULT NULL COMMENT '邮箱地址',
		  'name' 				varchar(150) DEFAULT NULL COMMENT '姓名',
		  'company' 			varchar(255) DEFAULT NULL COMMENT '公司',
		  'phone' 				varchar(100) DEFAULT NULL COMMENT '电话',
		  'fax' 				varchar(150) DEFAULT NULL COMMENT '传真',
		  'street1' 			text COMMENT '街道地址1',
		  'street2' 			varchar(255) DEFAULT NULL COMMENT '街道地址2',
		  'city' 				varchar(150) DEFAULT NULL COMMENT '城市',
		  'province' 			varchar(50)  DEFAULT NULL COMMENT '省/州',
		  'post' 				varchar(50)  DEFAULT NULL COMMENT '邮编',
		  'region'     			varchar(50)	 DEFAULT NULL COMMENT '收货人所在区',
		  'country' 			varchar(50)  DEFAULT NULL COMMENT '国家',
		  'customer_id' 		int(20) DEFAULT NULL COMMENT '用户的id',
		  'created_at' 			int(20) DEFAULT NULL COMMENT '创建时间戳',
		  'updated_at' 			int(20) DEFAULT NULL COMMENT '更新时间戳',
		  'is_default' 			int(11) NOT NULL DEFAULT '2' COMMENT '1代表是默认地址，2代表不是',
		  'idCardNo'			varchar(50) COMMENT '身份证号'
		  PRIMARY KEY ('address_id')


	order:	
		buyer_id 				STRING 			买家ID
		name 					STRING 			买家姓名
		phone 					STRING 			收货人电话
		address 				STRING 			收货地址
		province 				STRING 			收货人所在省
		city 					STRING 			收货人所在市
		region 					STRING 			收货人所在区
		post 					STRING 			邮政编码
		self_address 			STRING 			收货人的街道
		idCardNo 				STRING 			身份证号

		order_id 				String 			订单ID
 		seller_id				String 			卖家id
 		f_phone					String 			分销商手机号
		is_close				NUMBER 			订单是否可关闭	0（表示不可关闭）1（表示货到付款订单，可关闭）
		return_code				String 			退货单号
		status_ori 				STRING 			订单状态
 		add_time				String 			下单时间
 		updated_time 			String 			更新时间
 		express_fee_num			String 			运费，和express_fee相同
 		user_phone				String 			买家手机号
 		price 					String 			商品总价格，不包含运费
 		note 					String 			买家备注
 		seller_phone			String 			卖家手机号
		total_fee				String 			分成推广佣金
		seller_name 			String 			卖家微店名称
		order_type_des 			String 			订单类型描述
		argue_flag	   			NUMBER 			是否投诉
		f_shop_name 			STRING 			分销商店铺名称
		modify_price_enable		STRING 			是否可以改价0（表示不可改价）1（表示可以改价）
		fx_fee_value 			STRING 			分销商获取的分成金额

		express 				NULL			快递公司名称
		express_type 			String 			快递公司编号
		express_no				NULL 			快递单号
		send_time 				NULL 			发货时间
		quantity 				String 			商品总件数
		express_fee 			STRING 			快递费用

		confirm_expire			STRING 			担保交易过期时间
		order_type 				STRING 			订单类型编号，1为货到付款，2为直接交易，3为担保交易
		pay_time				STRING 			付款时间
		status_desc 			STRING 			订单状态详细描述
		express_note			String 			订单备注（卖家）

		original_total_price 	STRING 			订单原价格，包括运费 （此字段用于记录订单被创建时的价格,包含运费，减去优惠后的价格）

		total 					STRING 			订单总价 包含运费

		refund_status_ori 		STRING 			订单退款相关状态

		discount_list   		ARRAY (JSON)	优惠信息列表
			discount_info 		STRING 			优惠类型信息
			discount_type 		STRING 			101(店铺满包邮)102(满减)........

		full_gift_items			ARRAY（JSON）			满赠优惠的赠品，可以为空
			item_name 			STRING 			商品名称
			quantity 			STRING 			商品总件数
			item_id 			STRING 			商品id
			item_url 			STRING 			商品链接
			head_img 			String 			商品首图

	order_goods(订单商品表,生成订单时,对应的商品信息)
		item_name 				STRING 			商品名称
		total_price 			STRING 			商品对应总价，total_price=price*quantity
		sku_title 				STRING 			商品型号名称
		sku_merchant_code 		STRING 			商品型号编码
		fx_fee_rate 			STRING 			分销分成比例
		item_id 				STRING 			商品id
		merchant_code 			STRING 			商品编号
		price 					STRING 			价格
		quantity 				STRING 			商品数量
		url 					STRING 			商品URL
		img 					STRING 			商品图URL
		sku_id 					STRING 			商品型号ID

		deliver_status_desc 	STRING 			商品发货状态：待发货 | 已发货 ｜ ""（不在页面展示）
		deliver_id 				STRING 			发货批次编号，代表一次发货（注：不是物流单号）
		is_delivered 			NUMBER 			是否发货 1是0非
		can_deliver 			NUMBER 			商品是否可以发货 1是0非
		item_desc 				STRING 			商品描述
			

	退款信息（refund_info）：
		item_sku_id 			STRING 			sku ID
		refund_kind 			STRING 			退款类型，1表示退款，2表示退货退款
	 	can_refund 				STRING 			标识这个sku是否可发起商品退款
		refund_status_desc 		STRING 			状态描述
		item_id 				STRING 			商品id
		refund_fee 				STRING 			退款金额
		refund_express_fee 		STRING 			商品退款运费金额
		refund_item_fee 		STRING 			退款商品金额
		refund_status 			STRING 			退款状态
				1 申请退款或退款
				3 拒绝退款
				4 退货流程同意退货
				5 退货流程拒绝退货
				6 退货流程已提交退货物流信息
				9 退款取消
				10 退款完成（同意退款或收到退货后同意退款，退款流程完成，资金流程开始）
		refund_status_str 		STRING 			状态描述
		refund_no 				STRING 			退款唯一标识
		


